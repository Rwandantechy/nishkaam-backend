<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nishkaam Innovations Backend</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #280bcf72;
            color: #333;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }

       

        main {
            padding: 10%;
            font-size: 2rem;
            text-align: center;
            color: #fff;
            font-weight: bold;            
            animation: mainAnimation 9s infinite alternate ease-in-out;
        }

        h1 {
            text-align: center;
            background-color: #33333314;
            border-radius: 10px;
            margin: 0;
            padding: 1rem;
        }

        @keyframes headerAnimation {
            0%, 100% {
                transform: translateY(-50px);
            }
            50% {
                transform: translateY(50px);
            }
        }

        @keyframes mainAnimation {
            0%, 100% {
                transform: rotate(0deg) scale(1);
                opacity: 1;
            }
            50% {
                transform: rotate(0deg) scale(1.1);
                opacity: 0.7;
            }
        }

        h1 {
            text-align: center;
        }

        h1:hover {
            transform: scale(1.2);
        }
    </style>
</head>
<body>
    
    <main>
        <h1>The Backend Server is running and is ready to accept requests.
            👍
        </h1>
    </main>
</body>
</html>
